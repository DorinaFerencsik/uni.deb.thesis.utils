export enum Errors {
  InvalidCredentials = 'INVALID_CREDENTIALS',
  NotValidated = 'NOT_VALIDATED',
  TokenExpired = 'TOKEN_EXPIRED',
  InsufficentRole = 'INSUCCIFENT_ROLE',
  NotFound = 'NOT_FOUND',
}
